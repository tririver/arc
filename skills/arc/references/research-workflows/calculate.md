# Calculate Workflow

This workflow is a placeholder. It must not perform unsupported calculations or
claim results before the calculation procedure is implemented.

### Phase 1: Check Explicitness

Step 1: Read `<project-dir>/context.json`.

Step 2: Decide whether the user's idea is explicit enough to make an immediate
action plan with defined target quantity, assumptions, reference method, and
required inputs.

Step 3: If the idea is not explicit enough, return to Case 1 and Case 2 from
`SKILL.md` before proceeding.

### Phase 2: Stop Cleanly

Step 1: If the idea is explicit enough, summarize the missing implementation
status and the information already available.

Step 2: State that ARC calculation execution is not implemented yet.

Step 3: Do not invent equations, derivations, numerical results, or validation
claims.
